export interface ItemTabStatistic {
	total: number,
	newRecords: number,
	newRelations: number,
}

export const defaultItemTabStatistic: ItemTabStatistic = {
	total: 0,
	newRecords: 0,
	newRelations: 0,
}
